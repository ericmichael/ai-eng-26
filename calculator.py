"""Simple calculator utilities.

This repo previously included only an example calculator module under
`examples/`. Some exercises/tests expect a top-level `calculator.py`, so we
provide it here.
"""


def power(a, b):
    """Compute a to the power of b (a^b)."""

    return a**b

